<?xml version="1.0" encoding="UTF-8"?>
<definition xmlns="http://workflow.opencastproject.org">
  <id>whisper-start-transcription</id>
  <title>Start Whisper Transcription</title>
  <tags>
    <tag>archive</tag>
  </tags>
  <description>Start the Whisper transcription</description>

  <operations>

    <!-- Start Whisper transcription job -->
    <operation
      id="whisper-start-transcription"
      max-attempts="3"
      retry-strategy="hold"
      fail-on-error="true"
      exception-handler-workflow="partial-error"
      description="Start Whisper captions job">
      <configurations>
        <!--  Skip this operation if flavor already exists. Used for cases when mp already has captions. -->
        <configuration key="skip-if-flavor-exists">captions/source</configuration>
        <configuration key="language-code">en-US</configuration>
        <!-- Audio to be translated, produced in the previous compose operation -->
        <configuration key="source-flavor">presenter/source</configuration>
      </configurations>
    </operation>

  </operations>

</definition>

